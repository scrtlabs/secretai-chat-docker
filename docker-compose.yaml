services:
  secretai-streamlit:
    image: ghcr.io/scrtlabs/secretai-chat:latest
    container_name: secretai_streamlit
    env_file:
      - .env
    volumes:
      - ./app_data:/app/data
    ports:
      - '8501:8501'
    restart: unless-stopped
